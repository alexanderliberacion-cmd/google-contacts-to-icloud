name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
    steps:
      - uses: actions/checkout@b4ffde65f46336ab88eb53be808477a3836bae11 # v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065v5 # v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run tests (stdlib unittest — no dependencies)
        run: python tests/test_merge.py

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@b4ffde65f46336ab88eb53be808477a3836bae11 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065v5 # v5
        with:
          python-version: '3.12'
      - name: Install ruff
        run: pip install ruff==0.11.12
      - name: Lint
        run: ruff check .
